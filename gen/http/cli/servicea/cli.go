// Code generated by goa v2.0.0-wip, DO NOT EDIT.
//
// servicea HTTP client CLI support package
//
// Command:
// $ goa gen github.com/jimmiebtlr/goav2-example/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	apic "github.com/jimmiebtlr/goav2-example/gen/http/api/client"
	foobarc "github.com/jimmiebtlr/goav2-example/gen/http/foobar/client"
	widgetc "github.com/jimmiebtlr/goav2-example/gen/http/widget/client"
	goa "goa.design/goa"
	goahttp "goa.design/goa/http"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `api info
foobar list
widget list
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` api info` + "\n" +
		os.Args[0] + ` foobar list` + "\n" +
		os.Args[0] + ` widget list` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		apiFlags = flag.NewFlagSet("api", flag.ContinueOnError)

		apiInfoFlags = flag.NewFlagSet("info", flag.ExitOnError)

		foobarFlags = flag.NewFlagSet("foobar", flag.ContinueOnError)

		foobarListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		widgetFlags = flag.NewFlagSet("widget", flag.ContinueOnError)

		widgetListFlags = flag.NewFlagSet("list", flag.ExitOnError)
	)
	apiFlags.Usage = apiUsage
	apiInfoFlags.Usage = apiInfoUsage

	foobarFlags.Usage = foobarUsage
	foobarListFlags.Usage = foobarListUsage

	widgetFlags.Usage = widgetUsage
	widgetListFlags.Usage = widgetListUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if len(os.Args) < flag.NFlag()+3 {
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = os.Args[1+flag.NFlag()]
		switch svcn {
		case "api":
			svcf = apiFlags
		case "foobar":
			svcf = foobarFlags
		case "widget":
			svcf = widgetFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(os.Args[2+flag.NFlag():]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = os.Args[2+flag.NFlag()+svcf.NFlag()]
		switch svcn {
		case "api":
			switch epn {
			case "info":
				epf = apiInfoFlags

			}

		case "foobar":
			switch epn {
			case "list":
				epf = foobarListFlags

			}

		case "widget":
			switch epn {
			case "list":
				epf = widgetListFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if len(os.Args) > 2+flag.NFlag()+svcf.NFlag() {
		if err := epf.Parse(os.Args[3+flag.NFlag()+svcf.NFlag():]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "api":
			c := apic.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "info":
				endpoint = c.Info()
				data = nil
			}
		case "foobar":
			c := foobarc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data = nil
			}
		case "widget":
			c := widgetc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data = nil
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// apiUsage displays the usage of the api command and its subcommands.
func apiUsage() {
	fmt.Fprintf(os.Stderr, `API of service
Usage:
    %s [globalflags] api COMMAND [flags]

COMMAND:
    info: Show info of the service

Additional help:
    %s api COMMAND --help
`, os.Args[0], os.Args[0])
}
func apiInfoUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] api info

Show info of the service

Example:
    `+os.Args[0]+` api info
`, os.Args[0])
}

// foobarUsage displays the usage of the foobar command and its subcommands.
func foobarUsage() {
	fmt.Fprintf(os.Stderr, `Foobar microservice
Usage:
    %s [globalflags] foobar COMMAND [flags]

COMMAND:
    list: Show info of the service

Additional help:
    %s foobar COMMAND --help
`, os.Args[0], os.Args[0])
}
func foobarListUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] foobar list

Show info of the service

Example:
    `+os.Args[0]+` foobar list
`, os.Args[0])
}

// widgetUsage displays the usage of the widget command and its subcommands.
func widgetUsage() {
	fmt.Fprintf(os.Stderr, `Widgets microservice
Usage:
    %s [globalflags] widget COMMAND [flags]

COMMAND:
    list: Show info of the service

Additional help:
    %s widget COMMAND --help
`, os.Args[0], os.Args[0])
}
func widgetListUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] widget list

Show info of the service

Example:
    `+os.Args[0]+` widget list
`, os.Args[0])
}
